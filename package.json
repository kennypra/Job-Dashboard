{
  "name": "job-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Personal job-search dashboard: interview pipeline, STAR story bank, behavioral skills tracking, and technical prep.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
